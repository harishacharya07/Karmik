import 'package:flutter/material.dart';

class ElectricalShopScreen extends StatelessWidget {
  const ElectricalShopScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
