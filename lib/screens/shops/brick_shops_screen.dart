import 'package:flutter/material.dart';

class BrickShopScreen extends StatelessWidget {
  const BrickShopScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
