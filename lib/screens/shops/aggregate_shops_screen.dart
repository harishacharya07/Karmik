import 'package:flutter/material.dart';

class AggregateShopScreen extends StatelessWidget {
  const AggregateShopScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
