import 'package:flutter/material.dart';

class CreatorCommentScreen extends StatelessWidget {
  const CreatorCommentScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
