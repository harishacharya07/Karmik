import 'package:flutter/material.dart';

class CreatorDashScreen extends StatelessWidget {
  const CreatorDashScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
